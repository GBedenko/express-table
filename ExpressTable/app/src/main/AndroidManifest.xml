<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bedenko.genaro.expresstable">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewRestaurantActivity" />
        <activity android:name=".ChooseAccountTypeActivity" />
        <activity android:name=".ViewRestaurantActivity" />
        <activity android:name=".NewCustomerActivity" />
        <activity android:name=".SearchRestaurantsGpsActivity" />
        <activity android:name=".BookRestaurantActivity" />
        <activity android:name=".BookingConfirmationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CustomerDashboardActivity" />
        <activity android:name=".RestaurantDashboardActivity" />
        <activity android:name=".RestaurantReservationsActivity" />
        <activity android:name=".SearchRestaurantsListActivity"></activity>
    </application>

</manifest>